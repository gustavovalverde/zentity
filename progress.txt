## A2A Agent Card Discovery

### Phase 1: Agent Card Route — COMPLETE
- PRD: docs/plans/a2a-agent-card.md (Tasks 1.1–1.7)
- RFC: RFC-0033 §Phase 4
- Decisions:
  - Content-Type: `application/a2a+json` (IANA-registered per A2A spec §14.1.1)
  - A2A-Version header: ignored (discovery-only, no task server)
  - OAuth URLs derived from `getAuthIssuer()` + `joinAuthIssuerPath()` (consistent with existing well-known routes)
  - Security schemes: OAuth2 auth-code (PKCE) + OpenID Connect discovery
  - CORS: permissive (`*`) with A2A-specific headers
- Files:
  - NEW: apps/web/src/app/.well-known/agent-card.json/route.ts

### Phase 2: Integration Tests — COMPLETE
- PRD: docs/plans/a2a-agent-card.md (Tasks 2.1–2.6)
- 20 tests across 5 describe blocks: structure, security schemes, skills catalog, HTTP headers, URL resolution
- Follows `oidc-well-known.test.ts` pattern (dynamic import of route handler, no mocks)
- Files:
  - NEW: apps/web/src/lib/auth/__tests__/a2a-agent-card.integration.test.ts

## Gap Closure for Implemented Plans

### Phase 1: auth_time claim in ID tokens — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 1)
- Decisions:
  - `auth_time` sourced from `sessions.createdAt` (OIDC Core §2), not `Date.now()`
  - Falls back to `Date.now()` when no session exists (e.g., bare CIBA grants without prior session)
  - `getAssuranceForOAuth` return type extended to `AssuranceState & { authTime: number }`
  - Token-exchange path also fixed to use session-derived `authTime` instead of `now`
- Files:
  - MOD: apps/web/src/lib/assurance/data.ts — select `sessions.createdAt`, compute `authTime`
  - MOD: apps/web/src/lib/auth/auth.ts — emit `auth_time` in `customIdTokenClaims`
  - MOD: apps/web/src/lib/auth/oidc/token-exchange.ts — use `assurance.authTime` instead of `now`
  - MOD: apps/web/src/lib/auth/__tests__/assurance-id-token.integration.test.ts — stronger bounds assertions

### Phase 2: Discovery metadata completeness — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 2)
- No code changes needed — all metadata already emitted by `enrichDiscoveryMetadata`
- `grant_types_supported` includes token-exchange natively via `grantTypes` config
- Files:
  - MOD: apps/web/src/lib/auth/__tests__/oidc-well-known.test.ts — 3 new tests: acr_values, claims_supported, token-exchange grant

### Phase 5: Test hardening — CIBA & delegated auth — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 5)
- Decisions:
  - `customAccessTokenClaims` can't check grant_type (not in callback params). Identity-scope guard
    is functionally equivalent — only CIBA approval stages release handles. Added explicit comment.
  - Scope-guard test: stages handle, issues non-identity-scope token, verifies handle is preserved
  - Ping-while-poll test: ping reuses in-flight promise via inflightRef guard, no duplicate fetch
- Files:
  - MOD: apps/web/src/lib/auth/auth.ts — expanded comment on identity-scope proxy design
  - MOD: apps/web/src/lib/auth/__tests__/ciba-release-lifecycle.integration.test.ts — scope guard test
  - MOD: apps/demo-rp/src/hooks/use-ciba-flow.test.ts — ping-while-poll-in-flight regression test

### Phase 4a: RFC 8707 resource column + PAR persistence — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 4, items 1-2)
- Decisions:
  - Added `resource text("resource")` nullable column to `haipPushedRequests`
  - After hook persists `resource` from PAR body onto the record (plugin creates record, hook updates it)
  - PAR record is deleted during authorization by the HAIP resolver — resource must be read before that
- Files:
  - MOD: apps/web/src/lib/db/schema/haip.ts — added `resource` column
  - MOD: apps/web/src/lib/auth/auth.ts — after hook for PAR resource persistence + `desc` import
  - MOD: apps/web/src/lib/auth/__tests__/rfc8707-resource.integration.test.ts — persistence test

### Phase 3a: Shared assurance display component — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 3, items 1-2)
- Decisions:
  - Shared `AssuranceBadges` component using shadcn Badge + theme CSS vars (auto-adapts per data-theme)
  - AMR codes mapped to human-readable labels (pwd→Password, hwk→Hardware Key, etc.)
  - Aether `CibaResult` refactored to use shared component (eliminated inline badge rendering + TIER_RE)
- Files:
  - NEW: apps/demo-rp/src/components/shared/assurance-badges.tsx
  - MOD: apps/demo-rp/src/app/bank/page.tsx — added AssuranceBadges in dashboard header
  - MOD: apps/demo-rp/src/app/exchange/page.tsx — added AssuranceBadges in Account Status card
  - MOD: apps/demo-rp/src/app/wine/page.tsx — added AssuranceBadges after header
  - MOD: apps/demo-rp/src/app/aid/page.tsx — added AssuranceBadges in dashboard header
  - MOD: apps/demo-rp/src/components/aether/agent-chat.tsx — replaced inline badges with shared component

### Phase 3b: interaction_required error surfacing — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 3, item 2)
- Decisions:
  - popup-complete page forwards OAuth error params via postMessage
  - openOAuthPopup returns `{ completed, error?, errorDescription? }` instead of plain boolean
  - useOAuthFlow exposes `oauthError` + `dismissError` for all scenarios
  - Human-readable message map in useOAuthFlow for `interaction_required`
  - Exchange page shows dismissible error banner above main grid
- Files:
  - MOD: apps/demo-rp/src/app/oauth/popup-complete/page.tsx — forward error params
  - MOD: apps/demo-rp/src/lib/oauth-popup.ts — OAuthPopupResult type, error propagation
  - MOD: apps/demo-rp/src/hooks/use-oauth-flow.ts — error state, ERROR_MESSAGES map, runOAuthFlow
  - MOD: apps/demo-rp/src/app/exchange/page.tsx — error banner with dismiss

### Phase 3c: Aether token-exchange narration step — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 3, item 3)
- Decisions:
  - Separate narration bubble ("Narrowing permissions for the merchant API...") renders
    when exchangedTokens arrives, before CibaResult card
  - CibaResult text simplified to "Completing your purchase now." (no longer conditional)
- Files:
  - MOD: apps/demo-rp/src/components/aether/agent-chat.tsx — narration bubble + simplified CibaResult
