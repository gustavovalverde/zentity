# Tasks completed in this session

## Phase 1.1: Enable CIBA ping mode server-side (Plan §Phase 1, AC 1-5)
- Enabled `deliveryModes: ["poll", "ping"]` in ciba() plugin config
- Implemented `resolveClientNotificationEndpoint` — reads `backchannel_client_notification_endpoint` from client `metadata` JSON column
- Updated discovery metadata to advertise `["poll", "ping"]`
- 8 integration tests: endpoint resolution, fallback-to-poll, request-body override, ping-mode polling, authorization_pending, request creation, poll regression, missing-token fallback

# Key decisions made and reasoning (architectural decisions)
- `resolveClientNotificationEndpoint` queries `oauthClients.metadata` directly via Drizzle (already imported in auth.ts) rather than going through the better-auth adapter — simpler, no indirection
- Ping mode stores notification endpoint/token per CIBA request (not per client) — allows per-request endpoint override via `client_notification_uri` in bc-authorize body

# Files changed
- `apps/web/src/lib/auth/auth.ts` — ciba() config: deliveryModes + resolveClientNotificationEndpoint
- `apps/web/src/lib/auth/well-known-utils.ts` — discovery: ["poll"] → ["poll", "ping"]
- `apps/web/src/lib/auth/__tests__/ciba-ping.integration.test.ts` — new test file (8 tests)

## Phase 1.2: Demo-rp CIBA ping mode (Plan §Phase 1, AC 6-8)
- Added `cibaPings` table to demo-rp DB schema (auth_req_id, notification_token, received flag)
- Created callback endpoint `/api/ciba/callback` — verifies bearer token, marks ping received
- Updated BFF `/api/ciba` to generate per-request `client_notification_token`, send `client_notification_uri`, store in DB, add `check-ping` action
- Updated `use-ciba-flow.ts` — ping check runs every 2s alongside regular poll (5s). On ping received, immediately fetches tokens (fast path). Extracted `classifyPollResponse` to reduce cognitive complexity

## Phase 2.1: CIBA identity intent + staging endpoints (Plan §Phase 2, AC 6-7)
- Extracted `normalizeIdentityFields` + `IdentityFieldsSchema` into shared `identity-fields-schema.ts`
- Created `/api/ciba/identity/intent` — issues intent tokens bound to `auth_req_id` (not OAuth `sig`)
- Created `/api/ciba/identity/stage` — stages PII in ephemeral store, consumed by `customIdTokenClaims` during CIBA token minting
- Both endpoints validate session, CIBA request ownership, scope membership, intent token binding
- No changes needed to `customIdTokenClaims` — it already calls `consumeEphemeralClaimsByUser` which works for both OAuth and CIBA flows

## Phase 2.2: CIBA approval page server/client split (Plan §Phase 2, AC 8 prep)
- Converted CIBA approval page from monolithic "use client" to server+client pattern
- Server `page.tsx` calls `detectAuthMode(userId)` and passes `authMode` + `wallet` as props
- Client `ciba-approve-client.tsx` receives auth context for vault unlock rendering
- Added `layout.tsx` with `Web3Provider` wrapper (needed for wallet unlock path)

## Phase 2.3: Vault unlock + identity staging on CIBA approval page (Plan §Phase 2, AC 8-10)
- Extracted shared vault unlock components (`VaultErrorAlert`, `WalletVaultUnlockButton`, `OpaqueVaultUnlockForm`, `classifyVaultError`, `buildIdentityPayload`, `buildScopeKey`) into `src/components/vault-unlock.tsx`
- Updated OAuth consent page to import from shared module (no behavior change)
- Wired CIBA approval page with full vault unlock → intent → stage → approve flow
- Identity scopes in CIBA request trigger vault unlock UI (passkey/OPAQUE/wallet based on `authMode`)
- Staging calls `/api/ciba/identity/intent` then `/api/ciba/identity/stage` before approval
- Approve button disabled until vault is unlocked and intent token is valid (when identity scopes present)

## Phase 2.4: CIBA identity staging integration tests (Plan §Phase 2, AC 9-13)
- Added 3 integration tests to `ciba-token.integration.test.ts`:
  - PII claims in id_token when ephemeral claims are staged (identity.name + identity.dob)
  - No PII when no ephemeral claims are staged (openid-only scope)
  - Ephemeral claims are single-use (consumed after first token minting)
- Added `usedIntentJtis` cleanup to `resetDatabase()` test utility
- Added `resetEphemeralIdentityClaimsStore()` call in test beforeEach

## Phase 3.1: Approvals table schema (Plan §Phase 3, AC 1)
- Created `approvals` table schema in `src/lib/db/schema/approvals.ts`
- Columns: id, authReqId, userId, clientId, approvedScopes, authorizationDetails, encryptedPii, encryptionIv, releaseHandleHash (unique), status (approved|redeemed|expired), expiresAt, createdAt, redeemedAt
- Zero-knowledge at rest: release handle hash stored for lookup, actual key only in access token
- Added to DB connection schema and `resetDatabase()` test utility

## Phase 3.2: PII release endpoint + integration tests (Plan §Phase 3, AC 2-9, 11)
- `POST /api/oauth2/release` endpoint: verifies JWT access token via local JWKS lookup, extracts `release_handle` claim, decrypts PII, mints id_token, marks redeemed
- Token verification uses jose `jwtVerify` with `getLocalJwks()` (reads `jwks` table directly, fallback to JWKS URL fetch)
- PII filtering by approved scopes via `filterIdentityByScopes`
- Status lifecycle enforced: approved → redeemed (410 on second attempt), expired (410 with auto-transition)
- 8 integration tests: full redemption, one-time use, expired, missing handle, unknown handle, no bearer, authorization_details, scope filtering
- Tests use dedicated EdDSA key pair inserted into `jwks` table per test (avoids jwt-signer cache coupling)

# Files changed
- `apps/web/src/app/api/oauth2/release/route.ts` — new release endpoint
- `apps/web/src/lib/auth/__tests__/release-endpoint.integration.test.ts` — new test file (8 tests)

# Any blockers or notes for next iteration
- Next: Phase 3 remaining — wire release handle into CIBA grant handler (AC 3: embed in access token), replace ephemeral staging with durable path (AC 14)
