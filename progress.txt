## Domain Restructuring (docs/plans/domain-restructuring.md)

### Module 1.1: Magic-link nesting — COMPLETE
- PRD: Module 1 (page route restructuring), magic-link nesting
- Moved `(auth)/magic-link-sent/` → `(auth)/magic-link/sent/`
- Matches existing `forgot-password/sent/` parent/child pattern
- Zero references to old path found — no link/import updates needed
- Files:
  - MOVE: apps/web/src/app/(auth)/magic-link-sent/page.tsx → (auth)/magic-link/sent/page.tsx
- Pre-existing test failures: authorize-challenge + token-exchange integration tests (unrelated)

### Module 3.1: push-banner.tsx into _components — COMPLETE
- PRD: Module 3 (component extraction), push-banner convention fix
- Moved `dashboard/ciba/push-banner.tsx` → `dashboard/ciba/_components/push-banner.tsx`
- Updated import in `ciba/page.tsx`
- Only one consumer — clean move
- Note: human-models move DEFERRED — URL change from `/human-models/` to `/api/human-models/` could break Human.js client-side model loading silently. Needs investigation of how Human.js resolves model paths on the client.

### Module 1.2 + 6.1: Merge (2fa) into (auth) — COMPLETE
- PRD: Module 1 (merge (2fa) into (auth)) + Module 6 (layout dedup)
- Moved `backup-codes/` and `verify-2fa/` from `(2fa)/` to `(auth)/`
- Deleted `(2fa)/layout.tsx` — identical to `(auth)/layout.tsx` minus redirect
- Removed the completed-user redirect from `(auth)/layout.tsx` entirely:
  - `sign-up/page.tsx` already has its own server-side redirect
  - `sign-in/page.tsx` uses AuthUIProvider's client-side `redirectTo="/dashboard"`
  - Recovery/reset pages should stay accessible to logged-in users
- Layout is now a pure visual shell + provider wrapper (KISS)
- URL paths unchanged — route groups don't affect URLs
- Files:
  - DEL: apps/web/src/app/(2fa)/layout.tsx
  - MOVE: (2fa)/backup-codes/ → (auth)/backup-codes/
  - MOVE: (2fa)/verify-2fa/ → (auth)/verify-2fa/
  - MOD: (auth)/layout.tsx — removed redirect + unused imports

### Module 1.3: Developer consolidation — COMPLETE
- PRD: Module 1 (developer consolidation)
- Moved `dashboard/dev/` → `dashboard/developer/inspector/`
- Both dev tools now under `developer/` parent: `inspector/` and `applications/`
- Updated `app-sidebar.tsx` nav URL + `dynamic-breadcrumb.tsx` label/parent mapping
- Only 2 files referenced old path — clean move
- Files:
  - MOVE: dashboard/dev/page.tsx → dashboard/developer/inspector/page.tsx
  - MOD: components/dashboard/app-sidebar.tsx — URL update
  - MOD: components/dashboard/dynamic-breadcrumb.tsx — label + parent mapping

### Module 3.2: CibaApproveClient extraction — COMPLETE
- PRD: Module 3 (component extraction)
- Moved `dashboard/ciba/approve/ciba-approve-client.tsx` → `components/ciba/ciba-approve-client.tsx`
- Eliminated the only cross-route-group import (`approve/[authReqId]` → `(dashboard)`)
- All component imports use `@/` aliases — no relative import fixes needed
- Fixed Biome import order in ciba/approve/page.tsx (`@/components/*` before `@/lib/*`)
- Files:
  - MOVE: app/(dashboard)/dashboard/ciba/approve/ciba-approve-client.tsx → components/ciba/
  - MOD: app/(dashboard)/dashboard/ciba/approve/page.tsx — import path + order
  - MOD: app/approve/[authReqId]/page.tsx — import path

### Module 3.3: BindingAuthDialog move — COMPLETE
- PRD: Module 3 (component extraction)
- Moved `components/auth/binding-auth-dialog.tsx` → `components/verification/binding-auth-dialog.tsx`
- Only consumers: `verify/liveness` and `verify/passport-chip` — verification domain
- `components/auth/` retains 6 legitimate auth components (password forms, social buttons, etc.)
- Files:
  - MOVE: components/auth/binding-auth-dialog.tsx → components/verification/
  - MOD: verify/liveness/_components/liveness-verify-client.tsx — import path
  - MOD: verify/passport-chip/_components/zkpassport-flow.tsx — import path

### Module 5: tRPC crypto → zk rename — COMPLETE
- PRD: Module 5 (tRPC router renames)
- Renamed `routers/crypto/` → `routers/zk/`, `cryptoRouter` → `zkRouter`
- Updated 12 files: router registration, client usage, type aliases, tests, API route import
- Key: `RouterOutputs["crypto"]` bracket notation needed separate fix (sed missed it)
- `CryptoOutputs` type alias → `ZkOutputs` in zk/client.ts
- Biome auto-fixed import ordering in app.ts (zk sorted alphabetically after token)
- Files:
  - RENAME: lib/trpc/routers/crypto/ → zk/ (8 files)
  - MOD: lib/trpc/routers/app.ts — import + registration
  - MOD: lib/trpc/routers/credentials.ts — comment
  - MOD: lib/trpc/routers/__tests__/bbs.test.ts — import path
  - MOD: lib/trpc/routers/__tests__/proof-security.test.ts — import + variable name
  - MOD: lib/trpc/client.ts — doc comments
  - MOD: lib/privacy/zk/client.ts — trpc.zk.* + ZkOutputs type
  - MOD: lib/privacy/zk/binding-context.ts — trpc.zk.*
  - MOD: lib/privacy/fhe/__tests__/browser.test.ts — trpc.zk.*
  - MOD: app/api/crypto/nationality-proof/verify/route.ts — import path
  - MOD: app/(dashboard)/dashboard/developer/inspector/page.tsx — RouterOutputs["zk"]

### Module 6.2: Shared standalone layout — COMPLETE
- PRD: Module 6 (layout deduplication)
- Created `components/layouts/standalone-layout.tsx` — shared header + providers shell
- `approve/[authReqId]/layout.tsx` and `oauth/consent/layout.tsx` were identical
- Both now thin wrappers: read cookies → render `<StandaloneLayout>`
- Files:
  - NEW: components/layouts/standalone-layout.tsx
  - MOD: app/approve/[authReqId]/layout.tsx — simplified to 1 component render
  - MOD: app/oauth/consent/layout.tsx — same

### Module 1.4: Password recovery vertical slice — COMPLETE
- PRD: Module 1 (recovery vertical slice, password subtree)
- Moved `forgot-password/` → `recovery/password/`, `forgot-password/sent/` → `recovery/password/sent/`, `reset-password/` → `recovery/password/reset/`
- Updated 7 cross-references:
  - `forgot-password-form.tsx` — redirectTo + router.push URLs
  - `opaque-sign-in-form.tsx` — "Forgot password?" link
  - `auth-ui-provider.tsx` — viewPaths (FORGOT_PASSWORD + RESET_PASSWORD)
  - `opaque/server.ts` — buildResetUrl default fallback
  - `auth.ts` — disabledPaths
  - `eip712-bridge.tsx` — AUTH_PAGES_PATTERN regex
  - Internal links in `sent/page.tsx` and `reset/page.tsx`
- Files:
  - MOVE: (auth)/forgot-password/page.tsx → (auth)/recovery/password/page.tsx
  - MOVE: (auth)/forgot-password/sent/page.tsx → (auth)/recovery/password/sent/page.tsx
  - MOVE: (auth)/reset-password/page.tsx → (auth)/recovery/password/reset/page.tsx
  - MOD: components/auth/forgot-password-form.tsx — URLs
  - MOD: components/auth/opaque-sign-in-form.tsx — link href
  - MOD: components/providers/auth-ui-provider.tsx — viewPaths
  - MOD: components/providers/eip712-bridge.tsx — regex pattern
  - MOD: lib/auth/plugins/opaque/server.ts — default redirect URL
  - MOD: lib/auth/auth.ts — disabledPaths

### Module 1.5: Guardian recovery vertical slice — COMPLETE
- PRD: Module 1 (recovery vertical slice, guardian subtree)
- Moved `recover-social/` → `recovery/guardian/` (corrects misnomer — it's FROST guardian recovery, not social login)
- Moved `recover-guardian/` → `recovery/guardian/approve/`
- Updated 5 cross-references:
  - `sign-in/page.tsx` — "Lost your passkey?" link
  - `recovery-mailer.ts` — buildApprovalLink URL
  - `recover-social/page.tsx` (now guardian/page.tsx) — internal approval URLs
  - E2E `social-recovery.spec.ts` — RECOVERY_LINK_RE regex (escaped `/` in regex literal) + goto URLs
  - `README.md` — recovery routes table
- Files:
  - MOVE: (auth)/recover-social/page.tsx → (auth)/recovery/guardian/page.tsx
  - MOVE: (auth)/recover-guardian/page.tsx → (auth)/recovery/guardian/approve/page.tsx
  - MOVE: (auth)/recover-guardian/recover-guardian-client.tsx → (auth)/recovery/guardian/approve/
  - MOD: (auth)/sign-in/page.tsx — link href
  - MOD: lib/email/recovery-mailer.ts — approval URL
  - MOD: e2e/recovery/social-recovery.spec.ts — regex + goto URLs
  - MOD: README.md — route table

### Module 1.6: Passkey recovery + recovery root — COMPLETE
- PRD: Module 1 (recovery vertical slice, passkey subtree + root)
- Moved `recover-passkey/` → `recovery/passkey/`
- Moved `recover-account/` → `recovery/` (AuthView wrapper, becomes recovery root)
- Updated callbackURL self-reference in passkey page
- Updated RECOVER_ACCOUNT viewPath in auth-ui-provider
- Recovery tree now complete:
  ```
  (auth)/recovery/
    ├── page.tsx             ← recover-account (AuthView)
    ├── password/
    │   ├── page.tsx         ← forgot-password
    │   ├── sent/page.tsx    ← forgot-password/sent
    │   └── reset/page.tsx   ← reset-password
    ├── passkey/page.tsx     ← recover-passkey
    └── guardian/
        ├── page.tsx         ← recover-social
        └── approve/         ← recover-guardian
  ```
- Files:
  - MOVE: (auth)/recover-passkey/page.tsx → (auth)/recovery/passkey/page.tsx
  - MOVE: (auth)/recover-account/page.tsx → (auth)/recovery/page.tsx
  - MOD: recovery/passkey/page.tsx — callbackURL
  - MOD: components/providers/auth-ui-provider.tsx — viewPaths RECOVER_ACCOUNT

### Module 2.2: CIBA push move — COMPLETE
- PRD: Module 2 (API route consolidation), CIBA push co-location
- Moved `api/push/{subscribe,unsubscribe}/` → `api/ciba/push/{subscribe,unsubscribe}/`
- Moved test file alongside — relative imports preserved (`../subscribe/route`)
- Updated fetch URLs in `lib/push/client.ts`: `/api/push/*` → `/api/ciba/push/*`
- Updated test describe labels and Request URLs
- No references in `next.config.ts`, `push-sw.js`, or service worker — clean move
- `lib/push/` (client + server modules) and `lib/db/schema/push.ts` stay in place (library code, not routes)
- Files:
  - MOVE: app/api/push/subscribe/route.ts → app/api/ciba/push/subscribe/route.ts
  - MOVE: app/api/push/unsubscribe/route.ts → app/api/ciba/push/unsubscribe/route.ts
  - MOVE: app/api/push/__tests__/push-routes.integration.test.ts → app/api/ciba/push/__tests__/
  - MOD: lib/push/client.ts — fetch URLs updated
  - MOD: api/ciba/push/__tests__/push-routes.integration.test.ts — describe labels + Request URLs

## MCP Agent Server

### Phase 1: Project Bootstrap & Echo Tool — COMPLETE
- PRD: docs/plans/mcp-agent-server.md (Phase 1)
- Decisions:
  - Standalone app at `apps/mcp/` (no root workspace, matches monorepo pattern)
  - `WebStandardStreamableHTTPServerTransport` for Hono (not Node.js variant — Hono uses web standard Request/Response)
  - Server factory pattern: `createServer()` → `{ server, cleanup }`, transport-agnostic
  - One tool per file pattern: `tools/*.ts` each exports `register(server)` fn
  - CLI arg parsing: `--transport stdio|http`, `--port 3200`
  - Ultracite biome/core only (no react/next presets)
- Files:
  - NEW: apps/mcp/package.json, tsconfig.json, biome.json, vitest.config.mts, .gitignore
  - NEW: apps/mcp/src/index.ts — entry point with CLI arg dispatch
  - NEW: apps/mcp/src/config.ts — CLI arg + env var config
  - NEW: apps/mcp/src/server/index.ts — server factory
  - NEW: apps/mcp/src/tools/echo.ts — echo tool (dev/connectivity test)
  - NEW: apps/mcp/src/tools/index.ts — tool registration barrel
  - NEW: apps/mcp/src/transports/stdio.ts — StdioServerTransport wiring
  - NEW: apps/mcp/src/transports/http.ts — Hono + WebStandardStreamableHTTPServerTransport
  - NEW: apps/mcp/__tests__/server.test.ts — server factory tests (2)
  - NEW: apps/mcp/__tests__/tools/echo.test.ts — echo tool tests (3)

### Phase 2: OAuth Discovery & Client Registration — COMPLETE
- PRD: docs/plans/mcp-agent-server.md (Phase 2)
- Decisions:
  - Credentials stored at `~/.zentity/credentials.json` (0600 perms, dir 0700)
  - jose v6 needs `{ extractable: true }` for DPoP keypair generation
  - `WebStandardStreamableHTTPServerTransport` for Hono (consistent with Phase 1)
  - DCR registers as public client with CIBA + auth_code + refresh_token grant types
  - PRM endpoint at `/.well-known/oauth-protected-resource` (HTTP transport only)
  - DPoP private+public JWK both persisted for proof creation without re-import
- Files:
  - NEW: apps/mcp/src/auth/credentials.ts — StoredCredentials type, read/write/update
  - NEW: apps/mcp/src/auth/discovery.ts — OIDC discovery fetch + cache
  - NEW: apps/mcp/src/auth/dcr.ts — DCR registration with client_id persistence
  - NEW: apps/mcp/src/auth/dpop.ts — ES256 DPoP keypair gen + proof creation
  - NEW: apps/mcp/src/auth/resource-metadata.ts — RFC 9728 PRM response
  - MOD: apps/mcp/src/transports/http.ts — PRM endpoint wired in
  - NEW: apps/mcp/__tests__/auth/discovery.test.ts (4 tests)
  - NEW: apps/mcp/__tests__/auth/dcr.test.ts (4 tests)
  - NEW: apps/mcp/__tests__/auth/dpop.test.ts (6 tests)
  - NEW: apps/mcp/__tests__/auth/resource-metadata.test.ts (1 test)

## A2A Agent Card Discovery

### Phase 1: Agent Card Route — COMPLETE
- PRD: docs/plans/a2a-agent-card.md (Tasks 1.1–1.7)
- RFC: RFC-0033 §Phase 4
- Decisions:
  - Content-Type: `application/a2a+json` (IANA-registered per A2A spec §14.1.1)
  - A2A-Version header: ignored (discovery-only, no task server)
  - OAuth URLs derived from `getAuthIssuer()` + `joinAuthIssuerPath()` (consistent with existing well-known routes)
  - Security schemes: OAuth2 auth-code (PKCE) + OpenID Connect discovery
  - CORS: permissive (`*`) with A2A-specific headers
- Files:
  - NEW: apps/web/src/app/.well-known/agent-card.json/route.ts

### Phase 2: Integration Tests — COMPLETE
- PRD: docs/plans/a2a-agent-card.md (Tasks 2.1–2.6)
- 20 tests across 5 describe blocks: structure, security schemes, skills catalog, HTTP headers, URL resolution
- Follows `oidc-well-known.test.ts` pattern (dynamic import of route handler, no mocks)
- Files:
  - NEW: apps/web/src/lib/auth/__tests__/a2a-agent-card.integration.test.ts

## Gap Closure for Implemented Plans

### Phase 1: auth_time claim in ID tokens — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 1)
- Decisions:
  - `auth_time` sourced from `sessions.createdAt` (OIDC Core §2), not `Date.now()`
  - Falls back to `Date.now()` when no session exists (e.g., bare CIBA grants without prior session)
  - `getAssuranceForOAuth` return type extended to `AssuranceState & { authTime: number }`
  - Token-exchange path also fixed to use session-derived `authTime` instead of `now`
- Files:
  - MOD: apps/web/src/lib/assurance/data.ts — select `sessions.createdAt`, compute `authTime`
  - MOD: apps/web/src/lib/auth/auth.ts — emit `auth_time` in `customIdTokenClaims`
  - MOD: apps/web/src/lib/auth/oidc/token-exchange.ts — use `assurance.authTime` instead of `now`
  - MOD: apps/web/src/lib/auth/__tests__/assurance-id-token.integration.test.ts — stronger bounds assertions

### Phase 2: Discovery metadata completeness — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 2)
- No code changes needed — all metadata already emitted by `enrichDiscoveryMetadata`
- `grant_types_supported` includes token-exchange natively via `grantTypes` config
- Files:
  - MOD: apps/web/src/lib/auth/__tests__/oidc-well-known.test.ts — 3 new tests: acr_values, claims_supported, token-exchange grant

### Phase 5: Test hardening — CIBA & delegated auth — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 5)
- Decisions:
  - `customAccessTokenClaims` can't check grant_type (not in callback params). Identity-scope guard
    is functionally equivalent — only CIBA approval stages release handles. Added explicit comment.
  - Scope-guard test: stages handle, issues non-identity-scope token, verifies handle is preserved
  - Ping-while-poll test: ping reuses in-flight promise via inflightRef guard, no duplicate fetch
- Files:
  - MOD: apps/web/src/lib/auth/auth.ts — expanded comment on identity-scope proxy design
  - MOD: apps/web/src/lib/auth/__tests__/ciba-release-lifecycle.integration.test.ts — scope guard test
  - MOD: apps/demo-rp/src/hooks/use-ciba-flow.test.ts — ping-while-poll-in-flight regression test

### Phase 4a: RFC 8707 resource column + PAR persistence — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 4, items 1-2)
- Decisions:
  - Added `resource text("resource")` nullable column to `haipPushedRequests`
  - After hook persists `resource` from PAR body onto the record (plugin creates record, hook updates it)
  - PAR record is deleted during authorization by the HAIP resolver — resource must be read before that
- Files:
  - MOD: apps/web/src/lib/db/schema/haip.ts — added `resource` column
  - MOD: apps/web/src/lib/auth/auth.ts — after hook for PAR resource persistence + `desc` import
  - MOD: apps/web/src/lib/auth/__tests__/rfc8707-resource.integration.test.ts — persistence test

### Phase 3a: Shared assurance display component — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 3, items 1-2)
- Decisions:
  - Shared `AssuranceBadges` component using shadcn Badge + theme CSS vars (auto-adapts per data-theme)
  - AMR codes mapped to human-readable labels (pwd→Password, hwk→Hardware Key, etc.)
  - Aether `CibaResult` refactored to use shared component (eliminated inline badge rendering + TIER_RE)
- Files:
  - NEW: apps/demo-rp/src/components/shared/assurance-badges.tsx
  - MOD: apps/demo-rp/src/app/bank/page.tsx — added AssuranceBadges in dashboard header
  - MOD: apps/demo-rp/src/app/exchange/page.tsx — added AssuranceBadges in Account Status card
  - MOD: apps/demo-rp/src/app/wine/page.tsx — added AssuranceBadges after header
  - MOD: apps/demo-rp/src/app/aid/page.tsx — added AssuranceBadges in dashboard header
  - MOD: apps/demo-rp/src/components/aether/agent-chat.tsx — replaced inline badges with shared component

### Phase 3b: interaction_required error surfacing — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 3, item 2)
- Decisions:
  - popup-complete page forwards OAuth error params via postMessage
  - openOAuthPopup returns `{ completed, error?, errorDescription? }` instead of plain boolean
  - useOAuthFlow exposes `oauthError` + `dismissError` for all scenarios
  - Human-readable message map in useOAuthFlow for `interaction_required`
  - Exchange page shows dismissible error banner above main grid
- Files:
  - MOD: apps/demo-rp/src/app/oauth/popup-complete/page.tsx — forward error params
  - MOD: apps/demo-rp/src/lib/oauth-popup.ts — OAuthPopupResult type, error propagation
  - MOD: apps/demo-rp/src/hooks/use-oauth-flow.ts — error state, ERROR_MESSAGES map, runOAuthFlow
  - MOD: apps/demo-rp/src/app/exchange/page.tsx — error banner with dismiss

### Phase 3c: Aether token-exchange narration step — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 3, item 3)
- Decisions:
  - Separate narration bubble ("Narrowing permissions for the merchant API...") renders
    when exchangedTokens arrives, before CibaResult card
  - CibaResult text simplified to "Completing your purchase now." (no longer conditional)
- Files:
  - MOD: apps/demo-rp/src/components/aether/agent-chat.tsx — narration bubble + simplified CibaResult

### Phase 4b: Token endpoint resource validation — COMPLETE
- PRD: docs/plans/tasks/gap-closure-implemented-plans.md (Phase 4, remaining items)
- Decisions:
  - client_credentials: resource REQUIRED (no prior authorization step)
  - authorization_code: resource validated if present, NOT required
    (FPA challenge flow doesn't use PAR; standard flows require resource at PAR already)
  - CIBA: not affected (handles resource via cibaRequests.resource)
  - PAR-to-token binding (resource match) deferred — requires oauth-provider patch to propagate
    resource through verification value. PAR record deleted at authorize time.
  - `aud` binding already works natively via oauth-provider's `checkResource(ctx.body.resource)`
- Files:
  - MOD: apps/web/src/lib/auth/auth.ts — before hook for token endpoint resource validation
  - MOD: apps/web/src/lib/auth/__tests__/rfc8707-resource.integration.test.ts — 3 new tests
- Blockers:
  - PAR-to-token resource matching requires oauth-provider patch (verification value doesn't store resource)
