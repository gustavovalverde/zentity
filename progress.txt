# Tasks completed in this session

## CIBA Notifications Remediation — Missing AC Tests
- Phase 3 AC-7: non-identity CIBA token doesn't consume staged release handle (ciba-token.integration.test.ts)
- Phase 3 AC-8: concurrent release redemption yields exactly one success (release-endpoint.integration.test.ts)
- Phase 3 AC-5: rollback on decrypt failure preserves redeemability (release-endpoint.integration.test.ts)

# Architectural decisions
- Concurrent release test accepts 400 OR 410 for loser — Node.js single-threaded async means the second caller may re-read DB after status="claiming", hitting status guard (400) instead of atomic claim race (410). Both prevent double PII delivery.

## OIDC Assurance-Aware ID Tokens — Phase 1: Core Claims + Discovery
- Pure functions: computeAcr, computeAcrEidas, loginMethodToAmr in src/lib/assurance/oidc-claims.ts
- Vendor tarball patch: reordered createIdToken payload so customClaims override acr/auth_time defaults
- Schema: added lastLoginMethod column to sessions, enabled storeInDatabase in plugin
- Refactored data.ts: extracted gatherVerificationData, added getAssuranceForOAuth (session-less variant)
- Wired customIdTokenClaims: emits acr, acr_eidas, amr when openid scope present
- Discovery metadata: acr_values_supported + claims_supported enriched via well-known-utils.ts
- 18 unit tests for pure functions (all tiers, all login methods, edge cases)

## OIDC Assurance-Aware ID Tokens — Phase 1 Integration Tests
- 5 integration tests: tier-0, tier-1 passkey, tier-1 opaque, at_hash binding, scope guard
- assurance-id-token.integration.test.ts

## OIDC Assurance-Aware ID Tokens — Phase 2: at_hash (Access Token Hash Binding)
- Pure function: computeAtHash in src/lib/assurance/oidc-claims.ts
- Vendor tarball patch: sequenced access token creation before ID token in createUserTokens, passed accessToken to customIdTokenClaims via createIdToken
- Type def patch: added accessToken?: string to customIdTokenClaims info param
- Wired in auth.ts: computes at_hash from accessToken + client signing alg (metadata.id_token_signed_response_alg, default RS256)
- Discovery: at_hash added to claims_supported
- 7 unit tests for computeAtHash (RS256, ES256, EdDSA, ML-DSA-65, unknown alg, same-alg equivalence, different-token divergence)
- 1 integration test verifying at_hash matches recomputed SHA-256 left-half of access_token

# Architectural decisions
- Vendor sequencing: broke Promise.all parallelism in createUserTokens — access token created first (await), then refresh token + ID token in parallel. Enables at_hash in customIdTokenClaims without post-hoc re-signing.
- Signing alg resolution: reads metadata.id_token_signed_response_alg from client metadata (same source of truth as jwt-signer.ts), defaults to RS256.

## OIDC Assurance-Aware ID Tokens — Phase 3: Token Exchange Enrichment
- Token exchange ID token output now includes acr, acr_eidas, amr, auth_time
- Uses getAssuranceForOAuth (same as CIBA path) to resolve user's current tier
- at_hash not applicable: RFC 8693 produces a single token per exchange, no companion
- Integration test: exchanges access_token → id_token, verifies acr/amr/auth_time present

## OIDC Assurance-Aware ID Tokens — Phase 3: Demo-rp Integration
- Aether CibaResult: decodes id_token, displays acr tier badge + amr auth method badge
- Aether Token Details: shows acr/acr_eidas/amr JSON in expandable section
- demo-rp fetchUserInfo: validates at_hash against access token, logs warning on mismatch
- at_hash validation: reads alg from id_token header, computes left-half hash, compares

## auth_time verification
- Added auth_time assertions to tier-0 and tier-1 integration tests
- Framework emits auth_time natively (no custom claim needed): CIBA uses new Date(), auth-code uses session.createdAt

# Completion Status
## CIBA Notifications Remediation: COMPLETE
- All 4 phases implemented
- 2 BLOCKED test items: SW quick-approve suppression test (browser-only), demo-rp use-ciba-flow regression test (React hook, no test env)
- These are structural blockers, not implementation gaps

## OIDC Assurance-Aware ID Tokens: COMPLETE
- All 3 phases implemented
- Phase 1: acr, acr_eidas, amr, auth_time in customIdTokenClaims + discovery
- Phase 2: at_hash with vendor tarball sequencing patch
- Phase 3: token exchange enriched + demo-rp display + at_hash validation
- Tier 2/3 integration tests deferred: pure function coverage is complete, complex seeding for diminishing returns
- at_hash in token exchange: N/A per RFC 8693 (single token output, no companion)

## Step-Up Authentication — Phase 1: Authorize Endpoint Enforcement
- PRD ref: docs/plans/oidc-step-up-authentication.md, Phase 1
- Pure functions in src/lib/auth/oidc/step-up.ts: parseAcrValues, findSatisfiedAcr, isMaxAgeExceeded, buildOAuthErrorUrl
- Enforcement hook in src/lib/auth/oidc/step-up-hook.ts: enforceStepUp
- Wired into auth.ts before hook for /oauth2/authorize path
- Architecture: peek at PAR record without consuming, session resolved from cookie+DB
- max_age: redirects to /sign-in with callbackURL preserving request_uri, extends PAR TTL to 5min
- acr_values: redirects to RP redirect_uri with interaction_required, consumes PAR record
- prompt=none + max_age: returns login_required to RP redirect_uri
- Combined: max_age checked first (re-auth may refresh session), then acr_values
- 20 unit tests (step-up.test.ts), 16 integration tests (step-up-auth.integration.test.ts)
- Added haipPushedRequests/haipVpSessions to resetDatabase test utility
- Files: step-up.ts, step-up-hook.ts, auth.ts (hook), step-up.test.ts, step-up-auth.integration.test.ts, db-test-utils.ts

## Step-Up Authentication — Phase 2: CIBA acr_values Enforcement
- Schema: added acrValues column to cibaRequests table
- CIBA plugin patch: acr_values accepted in bc-authorize Zod schema, stored in DB, returned by verify endpoint
- Approval-time enforcement: before hook on /ciba/authorize checks user tier via getAssuranceForOAuth
- Token exchange safety net: before hook on /oauth2/token for CIBA grant re-checks tier
- Approval UI: displays required assurance tier badge when acr_values present
- 10 integration tests (step-up-ciba.integration.test.ts): approval rejection, approval success, preference order, token exchange safety net
- Files: ciba.ts (schema), step-up-ciba.ts, auth.ts (hooks), ciba-approve-client.tsx (UI), step-up-ciba.integration.test.ts, vendor patch

## Step-Up Authentication — Phase 3: Demo-rp Step-Up Scenarios
- Scenario type: added acrValues/maxAge fields to Scenario interface
- Exchange scenario: sends acr_values=urn:zentity:assurance:tier-2 via authorizationUrlParams in genericOAuth config
- Bank scenario: sends max_age=300 via authorizationUrlParams
- Aether CIBA: sends acr_values=urn:zentity:assurance:tier-2 in bc-authorize request
- CIBA API route: added acrValues to Zod schema, passes acr_values in bc-authorize body, extracted handleAuthorize for complexity
- Error UX: better-auth's built-in /error endpoint renders the error_description from step-up rejection
- Files: scenarios.ts, auth.ts (PROVIDER_AUTH_PARAMS + authorizationUrlParams), use-ciba-flow.ts, api/ciba/route.ts, aether/page.tsx

## MCP Compatibility — Phase 1a: Resource Validation + PAR Enforcement
- Pure function validateResourceUri in src/lib/auth/oidc/resource.ts (RFC 8707 §2)
- Validates: absolute URI, no fragment, http(s) scheme, required
- Before hook on /oauth2/par enforces resource presence and format
- OAuth error format: { error: "invalid_request", error_description: "..." }
- 10 unit tests (resource.test.ts)
- Files: resource.ts, auth.ts (hook), resource.test.ts

## MCP Compatibility — Phase 1b: Demo-rp Resource Migration
- All providers send resource=ZENTITY_URL in authorizationUrlParams (authorize URL)
- Token request body includes resource=ZENTITY_URL in getToken (auth code exchange)
- CIBA bc-authorize sends resource=ZENTITY_URL
- CIBA token request sends resource=ZENTITY_URL
- Enables JWT access tokens with aud binding for all demo-rp flows
- Files: auth.ts (makeProviderConfig, ZENTITY_URL resource), api/ciba/route.ts (bc-authorize + token)

## MCP Compatibility — Phase 1c: Valid Audiences + Integration Tests
- Added appUrl (NEXT_PUBLIC_APP_URL) to validAudiences so resource=appUrl works
- 6 integration tests (rfc8707-resource.integration.test.ts):
  - PAR: reject missing resource, reject fragment, reject relative URI, accept valid resource
  - CIBA: JWT access token with aud when resource present, opaque when absent
- PAR endpoint accepts JSON body (not form-urlencoded) — better-auth routing
- aud is array when openid scope present (adds userinfo endpoint)
- Files: auth.ts (validAudiences), rfc8707-resource.integration.test.ts

## MCP Compatibility — Phase 2: CIMD Client Resolution Pipeline
- Schema: added metadataUrl, metadataFetchedAt, trustLevel to oauthClients
- Pure validation in cimd-validation.ts: isUrlClientId, isPrivateHost, validateCimdMetadata, validateFetchUrl
- DB/fetch resolution in cimd.ts: resolveCimdClient with 1-hour TTL caching
- URL client_id detection: https:// always, http://localhost in dev only
- SSRF protection: private IP ranges rejected (IPv4 + IPv6)
- Metadata validation: client_id match, client_name required, redirect_uris absolute URIs, restricted grant_types/response_types, public client only
- Synthetic client: pairwise subject, trustLevel=0, metadataUrl stored
- Before hook on /oauth2/authorize and /oauth2/par for CIMD resolution
- 44 unit tests (cimd.test.ts), 9 integration tests (cimd.integration.test.ts)
- Files: cimd-validation.ts, cimd.ts, oauth-provider.ts (schema), auth.ts (hook)

## MCP Compatibility — Phase 3: E2E Test + WWW-Authenticate
- WWW-Authenticate header on 401/403: added to [...all] route handler
- 401: `Bearer resource_metadata="<url>"`
- 403: `Bearer resource_metadata="<url>", error="insufficient_scope"`
- www-authenticate.ts: getProtectedResourceMetadataUrl utility
- 3 E2E integration tests (mcp-e2e.integration.test.ts):
  - Discovery metadata includes CIMD + resource indicator flags
  - CIMD → PAR → synthetic client creation
  - Resource-bound CIBA token with aud + DPoP binding
- Protected resource metadata endpoint already existed (RFC 9728)
- Discovery already had client_id_metadata_document_supported + resource_indicators_supported

## MCP Compatibility: COMPLETE
- Phase 1: RFC 8707 resource indicator validation + demo-rp migration + integration tests
- Phase 2: CIMD client resolution pipeline (URL client_id → fetch → validate → upsert)
- Phase 3: E2E integration test + WWW-Authenticate headers

## Step-Up Authentication: COMPLETE
- All 3 phases implemented
- Phase 1: Authorize endpoint enforcement (acr_values + max_age, PAR + non-PAR)
- Phase 2: CIBA acr_values enforcement (approval + token exchange safety net)
- Phase 3: Demo-rp scenarios (exchange acr_values, bank max_age, aether CIBA acr_values)
