## Task 01: JWKS Plugin Encryption & OAuth Hardening
Status: Complete

### Changes
1. Removed `disablePrivateKeyEncryption: true` from jwt() plugin (auth.ts:1180)
   - Plugin's internal EdDSA key now encrypted at rest like all keys managed by key-vault.ts
2. Removed `force` param from cross-org client adoption (approve/route.ts)
   - Simplified: unowned→adopt, same-org→adopt, different-org→403
3. Added `at_hash` to token-exchange ID tokens (token-exchange.ts)
   - Binds ID token to subject access token per OIDC Core §3.3.2.11
   - Exported `getClientSigningAlg` from jwt-signer.ts for alg resolution
4. Tests: 2 new (at_hash present for AT→IDT, absent for IDT→IDT), 313 total pass

### Files changed
- apps/web/src/lib/auth/auth.ts — removed disablePrivateKeyEncryption
- apps/web/src/app/api/rp-admin/clients/approve/route.ts — simplified adoption logic
- apps/web/src/lib/auth/oidc/jwt-signer.ts — exported getClientSigningAlg
- apps/web/src/lib/auth/oidc/token-exchange.ts — at_hash in ID token output
- apps/web/src/lib/auth/__tests__/token-exchange.integration.test.ts — at_hash tests

## Task 02: Compliance Schema Cleanup & Server-Derived birthYearOffset
Status: Complete

### Changes
1. Removed `livenessPassed` and `faceMatchPassed` columns from identityVerificationDrafts
   - All read sites now derive from scores via policy thresholds (ANTISPOOF_REAL_THRESHOLD, ANTISPOOF_LIVE_THRESHOLD, FACE_MATCH_MIN_CONFIDENCE)
   - Write sites (upsertIdentityDraft, handler.ts, liveness.ts) no longer set these
   - Single source of truth: raw scores, not redundant booleans
2. Server-derived `birthYearOffset` in attestation.submit
   - Added `birthYearOffset` column to identity_verifications
   - OCR path: computed from dobDays during prepareDocument via new dobDaysToBirthYearOffset()
   - NFC path: computed from birthdate during submitResult
   - attestation.submit reads from verification record; client no longer supplies it
   - Client component (on-chain-attestation.tsx) no longer does vault unlock for birth year
3. Tests: 3 new (dobDaysToBirthYearOffset unit tests), all 313 pass

### Files changed
- apps/web/src/lib/db/schema/identity.ts — removed columns, added birthYearOffset
- apps/web/src/lib/db/queries/identity.ts — removed from upsertIdentityDraft
- apps/web/src/lib/identity/liveness/socket/handler.ts — removed livenessPassed write
- apps/web/src/lib/trpc/routers/liveness.ts — removed faceMatchPassed write
- apps/web/src/lib/trpc/routers/identity/helpers/job-processor.ts — derive from scores
- apps/web/src/lib/trpc/routers/identity/liveness-status.ts — derive from scores
- apps/web/src/lib/trpc/routers/identity/finalize.ts — derive from scores
- apps/web/src/lib/identity/verification/birth-year.ts — new dobDaysToBirthYearOffset
- apps/web/src/lib/trpc/routers/identity/prepare-document.ts — store birthYearOffset
- apps/web/src/lib/trpc/routers/passport-chip.ts — store birthYearOffset
- apps/web/src/lib/trpc/routers/attestation.ts — read from verification, not client
- apps/web/src/components/dashboard/on-chain-attestation.tsx — removed vault unlock

## Task 03: NFC Cross-Method Sybil Dedup
Status: Complete

### Changes
1. NFC chip path now computes `dedupKey` using same `computeDedupKey` as OCR
   - Extracts `document_number`, uses `issuingCountry`→`nationality` fallback for country
   - Calls `dedupKeyExistsForOtherUser` before creating verification
   - Stores `dedupKey` on verification record
2. Cross-method blocking: OCR→NFC or NFC→OCR on different accounts = CONFLICT
3. Same-user re-verification across methods = allowed (dedupKeyExistsForOtherUser checks userId)
4. `uniqueIdentifier` nullifier check still runs (defense in depth)
5. Tests: 4 new unit tests (sybil block, dedupKey storage, same-user allowed, no doc skips dedup)

### Files changed
- apps/web/src/lib/trpc/routers/passport-chip.ts — added dedup logic
- apps/web/src/lib/trpc/routers/__tests__/passport-chip.test.ts — dedup tests

## Task 09: PWA Install Banner
Status: Complete

### Changes
1. Created PwaInstallBanner component alongside existing push-banner
   - Chromium: captures `beforeinstallprompt`, triggers native install dialog
   - iOS: shows "Share → Add to Home Screen" instructions
   - Already-installed detection via `display-mode: standalone` + `navigator.standalone`
   - 30-day localStorage dismissal cooldown
   - Graceful fallback: hidden on Firefox/Safari desktop (no `beforeinstallprompt`)
2. Rendered on CIBA listing page after PushNotificationBanner

### Files changed
- apps/web/src/app/(dashboard)/dashboard/ciba/_components/pwa-install-banner.tsx — new
- apps/web/src/app/(dashboard)/dashboard/ciba/page.tsx — added PwaInstallBanner

## Task 08 (Part 1): Consent Encryption Badge & Shield Color
Status: Complete (parts 1-2 of 4; parts 3-4 = claims routing = separate commit)

### Changes
1. Added Data Encryption badge to `deriveSecurityBadges`
   - Queries `rpEncryptionKeys` for active compliance key on consent page
   - "Encrypted" (AES-GCM standard) / "PQ Encrypted" (ML-KEM-768)
2. Added `computeShieldColor` + `getShieldIcon` for shield color derivation
   - Green: PQ signing OR PQ encryption + pairwise
   - Yellow: modern signing or standard encryption (not full PQ)
   - Gray: baseline (RS256, no encryption, public subject)
   - Shield icon: ShieldCheck (green), Shield (yellow), ShieldAlert (gray)
3. Tests: 6 new tests (encryption badges + shield color logic), all pass

### Files changed
- apps/web/src/app/oauth/consent/_components/client-security-badges.tsx — badge + shield logic
- apps/web/src/app/oauth/consent/page.tsx — query rpEncryptionKeys
- apps/web/src/app/oauth/consent/__tests__/security-badges.test.ts — updated tests

## Task 08 (Part 2): PII Routing to Userinfo via Claims Parameter
Status: Complete

### Changes
1. `customIdTokenClaims` now PEEKS ephemeral PII (non-destructive)
   - PII stays available for userinfo delivery
   - `claims.id_token` filter still applied
2. `customUserInfoClaims` now CONSUMES ephemeral PII (terminal consumer)
   - When `claims.userinfo` requests identity fields, PII delivered via userinfo
   - Backward compat: no `claims` param → PII only in id_token
3. Added `peekEphemeralClaimsByUser` to ephemeral-identity-claims.ts
4. Tests: 3 new (peek vs consume, cross-client safety)

### Architectural decision
- id_token hook peeks, userinfo hook consumes — userinfo is terminal consumer
- If RP never calls userinfo, 5-min TTL cleans up ephemeral PII
- Cross-client safety guard preserved in peek (returns null for 2+ entries)

### Files changed
- apps/web/src/lib/auth/oidc/ephemeral-identity-claims.ts — peekEphemeralClaimsByUser
- apps/web/src/lib/auth/auth.ts — peek in customIdTokenClaims, consume+route in customUserInfoClaims
- apps/web/src/lib/auth/oidc/__tests__/claims-delivery.test.ts — new test

## Task 04: On-Chain Revocation Cascade & Admin Credential Revocation
Status: Complete

### Changes
1. Wired actual on-chain revocation in revokeIdentity step 4
   - Calls provider.revokeAttestation(walletAddress) for each confirmed attestation
   - Success → "revoked"; failure → "revocation_pending" (non-blocking)
2. Added "revocation_pending" to AttestationStatus enum + schema
3. Reconciliation function: reconcilePendingRevocations() with exponential backoff (1s, 3s, 9s, max 3 retries)
4. Admin procedure: admin.retryPendingRevocations
5. Individual credential revocation: identity.revokeCredential({ credentialId })
   - Revokes single OID4VCI credential without full cascade
   - Sets status=1 + revokedAt

### Files changed
- apps/web/src/lib/db/schema/attestation.ts — revocation_pending enum
- apps/web/src/lib/db/queries/identity.ts — on-chain call in revokeIdentity
- apps/web/src/lib/db/queries/attestation.ts — reconcilePendingRevocations
- apps/web/src/lib/trpc/routers/admin.ts — retryPendingRevocations procedure
- apps/web/src/lib/trpc/routers/identity/revoke.ts — revokeCredentialProcedure
- apps/web/src/lib/trpc/routers/identity/router.ts — wire revokeCredential
- apps/web/src/components/dashboard/on-chain-attestation.tsx — revocation_pending status
- apps/web/src/lib/db/queries/__tests__/revocation.integration.test.ts — updated

## Task 10: Structured Agent Claims (Tier 1)
Status: Complete

### Changes
1. CIBA plugin updated (better-auth source) to accept `agent_claims` on bc-authorize
   - Body schema: `agent_claims: z.string().optional()`
   - DB insert: stores `agentClaims`
   - sendNotification: passes `agentClaims` to hooks
   - Verify endpoint: returns parsed `agent_claims` in response
   - Grant handler (poll + push): injects `agent` into access token JWT
   - Also fixed authReqId binding in both poll and push grant handlers
2. Rebuilt CIBA tarball with all changes baked in (no patch needed)
3. Schema: `agentClaims` text column on `cibaRequests`
4. AgentIdentityCard component with "Unverified" badge
5. Push notification title: "[Agent Name] requests approval"
6. Approval page: server-side agent claims query + prop
7. auth.ts: parses agentClaims JSON in sendNotification callback, passes agentName to push

### Files changed
- apps/web/vendor/better-auth-ciba-1.5.1-beta.3.tgz — rebuilt with agent_claims support
- apps/web/vendor/@better-auth__ciba@1.5.1-beta.3.patch — removed (changes in tarball)
- apps/web/package.json — removed CIBA patch entry from patchedDependencies
- apps/web/src/lib/db/schema/ciba.ts — agentClaims column
- apps/web/src/lib/auth/auth.ts — agentName extraction in sendNotification
- apps/web/src/components/ciba/ciba-approve-client.tsx — AgentIdentityCard
- apps/web/src/app/approve/[authReqId]/page.tsx — server-side query
- apps/web/src/lib/push/ciba-payload.ts — agent name in push title

## Task 06: Custodial FROST Email-Triggered Signing
Status: Complete (web-side wiring; signer binary unchanged)

### Changes
1. `signRecoveryChallenge` accepts `endpointOverrides` param
   - Overlays custom signer endpoints by participant index
   - Used when CUSTODIAL_SIGNER_URL is configured
2. `approveGuardianProcedure` detects custodialEmail guardians
   - Passes CUSTODIAL_SIGNER_URL as endpoint override for the custodial guardian's participant index
   - Custodial signer receives the same guardian_assertion JWT as all other signers
3. Non-custodial guardian flow unaffected (no endpointOverrides = existing behavior)

### Architectural decision
- endpointOverrides Map injected at the challenge level, not at frost-service global config
- Custodial signer's participant index resolved from DB guardian record, not from CUSTODIAL_SIGNER_ID
- CUSTODIAL_SIGNER_ID remains for signer identity correlation in logging/admin UI (future use)

### Files changed
- apps/web/src/lib/recovery/frost-service.ts — endpointOverrides param
- apps/web/src/lib/trpc/routers/recovery/challenge.ts — custodial signer wiring

## Task 07 (Part 4): BCL Integration Tests
Status: Complete (part 4 of 4; parts 1-3 = demo-rp changes = separate)

### Changes
1. Comprehensive BCL integration tests (8 tests):
   - Logout token delivery to BCL-registered clients
   - JWT structure validation (iss, sub, aud, iat, jti, events)
   - sid included when backchannel_logout_session_required: true
   - sid omitted when not required
   - Clients without backchannel_logout_uri skipped
   - jti uniqueness across logout events
   - revokePendingCibaOnLogout rejects pending requests
   - revokePendingCibaOnLogout leaves non-pending requests unchanged

### Files changed
- apps/web/src/lib/auth/oidc/__tests__/bcl-integration.test.ts — new

## Task 07 (Parts 1+3): Demo-rp BCL Endpoint + DCR Registration
Status: Complete

### Changes
1. BCL endpoint at /api/auth/backchannel-logout in demo-rp
   - Accepts POST application/x-www-form-urlencoded with logout_token JWT
   - Verifies JWT via Zentity's JWKS (createRemoteJWKSet)
   - Validates BCL event claim (OIDC BCL §2.4)
   - Extracts sub → finds local user via account.accountId → deletes all sessions
   - Returns 200 on success
2. DCR registration includes backchannel_logout_uri
   - Added to /api/dcr/route.ts body sent to Zentity's DCR endpoint

### Notes
- Task 07 Part 2 (UI: "Session ended by Zentity" banner + "Logout from Zentity" button) deferred — per-scenario UI changes across 5 demo scenarios (bank/exchange/wine/aid/aether) require deeper investigation of each scenario's auth flow

### Files changed
- apps/demo-rp/src/app/api/auth/backchannel-logout/route.ts — new BCL endpoint
- apps/demo-rp/src/app/api/dcr/route.ts — backchannel_logout_uri in registration

## Task 05: HPKE Key Authentication in FROST DKG (TOFU scaffolding)
Status: Complete (TOFU bootstrap; Schnorr signing deferred to multi-DKG scenario)

### Changes
1. Signer (Rust): /signer/info returns hpke_pubkey_signature (null) + hpke_pubkey_verified (false)
   - has_any_key_shares() checks if FROST key shares exist in ReDB storage
   - TOFU warning logged when no prior key shares (initial DKG)
2. Web server: frost-service.ts SignerInfo interface includes signature/verified fields
   - Logs warning for unverified HPKE keys during DKG
3. Cargo check passes, 11 Rust tests pass

### Architectural decision
- TOFU model for initial DKG (recommended by task) — no prior FROST key = accept unsigned
- Schnorr single-party signing with FROST key shares is complex and only needed for
  multi-DKG scenarios (not yet implemented in the application)
- The scaffolding is in place: when has_any_key_shares() returns true, the signer can
  compute hpke_pubkey_signature using its stored FROST signing share

### Files changed
- apps/signer/src/routes/signer_routes.rs — /signer/info returns signature fields
- apps/signer/src/frost/signer_logic.rs — has_any_key_shares() method
- apps/signer/src/storage/redb.rs — has_any_shares() storage check
- apps/web/src/lib/recovery/frost-service.ts — SignerInfo interface + TOFU logging

## Userinfo-Only PII Delivery: Task 01
Status: Complete

### Changes
1. Removed identity PII from `customIdTokenClaims` — zero identity fields emitted
   - Removed ephemeral peek, identityClaims assembly
   - Proof claims, assurance, at_hash, sid unchanged
2. Made `customUserInfoClaims` the sole PII consumer
   - Consumes ephemeral claims regardless of claims parameter
   - `claims.userinfo` acts as data minimization filter only
3. Removed `release_handle` from `customAccessTokenClaims`
   - No longer consumes release handles for CIBA identity flows
4. CIBA staging route switched from approvals-table encryption to `storeEphemeralClaims` with 10-min TTL
5. Added TTL parameter to `storeEphemeralClaims` (default 5 min, CIBA 10 min)
6. Deleted `peekEphemeralClaimsByUser` (single consumer model)
7. Deleted files: approval-crypto.ts, ephemeral-release-handles.ts, approvals.ts schema, /api/oauth2/release route
8. Deleted tests: ciba-release-lifecycle, release-endpoint, approval-crypto tests
9. Updated: ciba-token tests (removed release_handle tests), intent-binding tests (removed approvals assertions), claims-delivery tests (removed peek tests)
10. Removed approvals schema from db connection and test utils
11. Removed oauth2ReleaseLimiter from rate-limiters
12. Tests: 286 pass, typecheck clean, lint clean

### Files changed
- apps/web/src/lib/auth/oidc/ephemeral-identity-claims.ts — TTL param, deleted peek
- apps/web/src/lib/auth/auth.ts — removed PII from id_token, removed release_handle, userinfo-only delivery
- apps/web/src/app/api/ciba/identity/stage/route.ts — ephemeral store instead of approvals
- apps/web/src/lib/db/connection.ts — removed approvals schema
- apps/web/src/test/db-test-utils.ts — removed approvals
- apps/web/src/lib/utils/rate-limiters.ts — removed oauth2ReleaseLimiter
- Deleted: approval-crypto.ts, ephemeral-release-handles.ts, approvals.ts, release/route.ts, 3 test files

## Userinfo-Only PII Delivery: Task 02
Status: Complete

### Changes
1. Demo-rp `fetchUserInfo`: inverted merge priority — userinfo wins over id_token for PII fields
2. Demo-rp CIBA route: added "userinfo" action to call Zentity userinfo endpoint
3. Demo-rp `use-ciba-flow.ts`: calls userinfo after token receipt, exposes `userInfo` state
4. Demo-rp `agent-chat.tsx`: displays userinfo identity claims in token details panel
5. Demo-rp `aether/page.tsx`: threads `userInfo` from hook to AgentChat component
6. `docs/oauth-integrations.md`: removed release endpoint from table, updated identity delivery to userinfo-only, updated disclosure paths, updated zero persistent PII
7. `docs/agentic-authorization.md`: replaced release handle section with ephemeral userinfo delivery, updated Flow 2/3 sequence diagrams, updated security properties, updated claims/standards tables
8. `CLAUDE.md`: replaced release handle triple binding with ephemeral userinfo delivery description

### Files changed
- apps/demo-rp/src/lib/auth.ts — inverted fetchUserInfo merge priority
- apps/demo-rp/src/app/api/ciba/route.ts — added userinfo action
- apps/demo-rp/src/hooks/use-ciba-flow.ts — fetchUserInfo + userInfo state
- apps/demo-rp/src/components/aether/agent-chat.tsx — userInfo display
- apps/demo-rp/src/app/aether/page.tsx — thread userInfo prop
- docs/oauth-integrations.md — userinfo-only delivery
- docs/agentic-authorization.md — removed release handles, updated flows
- CLAUDE.md — updated CIBA description
