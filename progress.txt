## Task 01: JWKS Plugin Encryption & OAuth Hardening
Status: Complete

### Changes
1. Removed `disablePrivateKeyEncryption: true` from jwt() plugin (auth.ts:1180)
   - Plugin's internal EdDSA key now encrypted at rest like all keys managed by key-vault.ts
2. Removed `force` param from cross-org client adoption (approve/route.ts)
   - Simplified: unowned→adopt, same-org→adopt, different-org→403
3. Added `at_hash` to token-exchange ID tokens (token-exchange.ts)
   - Binds ID token to subject access token per OIDC Core §3.3.2.11
   - Exported `getClientSigningAlg` from jwt-signer.ts for alg resolution
4. Tests: 2 new (at_hash present for AT→IDT, absent for IDT→IDT), 313 total pass

### Files changed
- apps/web/src/lib/auth/auth.ts — removed disablePrivateKeyEncryption
- apps/web/src/app/api/rp-admin/clients/approve/route.ts — simplified adoption logic
- apps/web/src/lib/auth/oidc/jwt-signer.ts — exported getClientSigningAlg
- apps/web/src/lib/auth/oidc/token-exchange.ts — at_hash in ID token output
- apps/web/src/lib/auth/__tests__/token-exchange.integration.test.ts — at_hash tests

## Task 02: Compliance Schema Cleanup & Server-Derived birthYearOffset
Status: Complete

### Changes
1. Removed `livenessPassed` and `faceMatchPassed` columns from identityVerificationDrafts
   - All read sites now derive from scores via policy thresholds (ANTISPOOF_REAL_THRESHOLD, ANTISPOOF_LIVE_THRESHOLD, FACE_MATCH_MIN_CONFIDENCE)
   - Write sites (upsertIdentityDraft, handler.ts, liveness.ts) no longer set these
   - Single source of truth: raw scores, not redundant booleans
2. Server-derived `birthYearOffset` in attestation.submit
   - Added `birthYearOffset` column to identity_verifications
   - OCR path: computed from dobDays during prepareDocument via new dobDaysToBirthYearOffset()
   - NFC path: computed from birthdate during submitResult
   - attestation.submit reads from verification record; client no longer supplies it
   - Client component (on-chain-attestation.tsx) no longer does vault unlock for birth year
3. Tests: 3 new (dobDaysToBirthYearOffset unit tests), all 313 pass

### Files changed
- apps/web/src/lib/db/schema/identity.ts — removed columns, added birthYearOffset
- apps/web/src/lib/db/queries/identity.ts — removed from upsertIdentityDraft
- apps/web/src/lib/identity/liveness/socket/handler.ts — removed livenessPassed write
- apps/web/src/lib/trpc/routers/liveness.ts — removed faceMatchPassed write
- apps/web/src/lib/trpc/routers/identity/helpers/job-processor.ts — derive from scores
- apps/web/src/lib/trpc/routers/identity/liveness-status.ts — derive from scores
- apps/web/src/lib/trpc/routers/identity/finalize.ts — derive from scores
- apps/web/src/lib/identity/verification/birth-year.ts — new dobDaysToBirthYearOffset
- apps/web/src/lib/trpc/routers/identity/prepare-document.ts — store birthYearOffset
- apps/web/src/lib/trpc/routers/passport-chip.ts — store birthYearOffset
- apps/web/src/lib/trpc/routers/attestation.ts — read from verification, not client
- apps/web/src/components/dashboard/on-chain-attestation.tsx — removed vault unlock

## Task 03: NFC Cross-Method Sybil Dedup
Status: Complete

### Changes
1. NFC chip path now computes `dedupKey` using same `computeDedupKey` as OCR
   - Extracts `document_number`, uses `issuingCountry`→`nationality` fallback for country
   - Calls `dedupKeyExistsForOtherUser` before creating verification
   - Stores `dedupKey` on verification record
2. Cross-method blocking: OCR→NFC or NFC→OCR on different accounts = CONFLICT
3. Same-user re-verification across methods = allowed (dedupKeyExistsForOtherUser checks userId)
4. `uniqueIdentifier` nullifier check still runs (defense in depth)
5. Tests: 4 new unit tests (sybil block, dedupKey storage, same-user allowed, no doc skips dedup)

### Files changed
- apps/web/src/lib/trpc/routers/passport-chip.ts — added dedup logic
- apps/web/src/lib/trpc/routers/__tests__/passport-chip.test.ts — dedup tests

## Task 09: PWA Install Banner
Status: Complete

### Changes
1. Created PwaInstallBanner component alongside existing push-banner
   - Chromium: captures `beforeinstallprompt`, triggers native install dialog
   - iOS: shows "Share → Add to Home Screen" instructions
   - Already-installed detection via `display-mode: standalone` + `navigator.standalone`
   - 30-day localStorage dismissal cooldown
   - Graceful fallback: hidden on Firefox/Safari desktop (no `beforeinstallprompt`)
2. Rendered on CIBA listing page after PushNotificationBanner

### Files changed
- apps/web/src/app/(dashboard)/dashboard/ciba/_components/pwa-install-banner.tsx — new
- apps/web/src/app/(dashboard)/dashboard/ciba/page.tsx — added PwaInstallBanner

## Task 08 (Part 1): Consent Encryption Badge & Shield Color
Status: Complete (parts 1-2 of 4; parts 3-4 = claims routing = separate commit)

### Changes
1. Added Data Encryption badge to `deriveSecurityBadges`
   - Queries `rpEncryptionKeys` for active compliance key on consent page
   - "Encrypted" (AES-GCM standard) / "PQ Encrypted" (ML-KEM-768)
2. Added `computeShieldColor` + `getShieldIcon` for shield color derivation
   - Green: PQ signing OR PQ encryption + pairwise
   - Yellow: modern signing or standard encryption (not full PQ)
   - Gray: baseline (RS256, no encryption, public subject)
   - Shield icon: ShieldCheck (green), Shield (yellow), ShieldAlert (gray)
3. Tests: 6 new tests (encryption badges + shield color logic), all pass

### Files changed
- apps/web/src/app/oauth/consent/_components/client-security-badges.tsx — badge + shield logic
- apps/web/src/app/oauth/consent/page.tsx — query rpEncryptionKeys
- apps/web/src/app/oauth/consent/__tests__/security-badges.test.ts — updated tests

## Task 08 (Part 2): PII Routing to Userinfo via Claims Parameter
Status: Complete

### Changes
1. `customIdTokenClaims` now PEEKS ephemeral PII (non-destructive)
   - PII stays available for userinfo delivery
   - `claims.id_token` filter still applied
2. `customUserInfoClaims` now CONSUMES ephemeral PII (terminal consumer)
   - When `claims.userinfo` requests identity fields, PII delivered via userinfo
   - Backward compat: no `claims` param → PII only in id_token
3. Added `peekEphemeralClaimsByUser` to ephemeral-identity-claims.ts
4. Tests: 3 new (peek vs consume, cross-client safety)

### Architectural decision
- id_token hook peeks, userinfo hook consumes — userinfo is terminal consumer
- If RP never calls userinfo, 5-min TTL cleans up ephemeral PII
- Cross-client safety guard preserved in peek (returns null for 2+ entries)

### Files changed
- apps/web/src/lib/auth/oidc/ephemeral-identity-claims.ts — peekEphemeralClaimsByUser
- apps/web/src/lib/auth/auth.ts — peek in customIdTokenClaims, consume+route in customUserInfoClaims
- apps/web/src/lib/auth/oidc/__tests__/claims-delivery.test.ts — new test
