[build]
builder = "DOCKERFILE"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 300
restartPolicyType = "ALWAYS"
numReplicas = 1

# Explicitly set the port Railway should use for healthchecks
[[deploy.port]]
protocol = "http"
port = 5004

