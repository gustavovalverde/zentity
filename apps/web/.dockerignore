# https://docs.docker.com/engine/reference/builder/#dockerignore-file
#
# Exclude everything first, then whitelist only what's needed for the build.
# This keeps the Docker context small and build fast.

# Exclude everything
*

# Include package manifests
!package.json
!bun.lock

# Include source code
!src/

# Include public assets
!public/

# Include configuration files
!next.config.ts
!drizzle.config.ts
!tsconfig.json
!postcss.config.mjs
!tailwind.config.ts
!components.json

# Include scripts (entrypoint)
!scripts/

# Include Noir circuits (needed for ZK proofs)
!noir-circuits/

# Include Dockerfile and railway config (for reference, not used in build)
!Dockerfile
!railway.toml
