/**
 * Liveness Detection Module
 *
 * Provides client-side liveness detection using Human.js for multi-gesture challenges.
 */

// Re-export constants that consumers might need
export { STABILITY_FRAMES } from "./constants";

// Re-export types for consumers

// Re-export the main hook
export { useSelfieLivenessFlow } from "./use-selfie-liveness-flow";
