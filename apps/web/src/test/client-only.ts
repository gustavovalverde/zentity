// Vitest stub for Next.js `client-only`.
//
// Next.js resolves `client-only` via bundler conditions. In Vitest/Vite, we
// alias it to an empty module so client modules can be imported in unit tests.
export {};
