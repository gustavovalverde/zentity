// Vitest stub for Next.js `server-only`.
//
// Next.js resolves `server-only` via bundler conditions. In Vitest/Vite, we
// alias it to an empty module so server modules can be imported in unit tests.
export {};
