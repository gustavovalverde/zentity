diff --git a/dist/index-FxeUTEK6.d.mts b/dist/index-FxeUTEK6.d.mts
index 3139ac4..0af24e9 100644
--- a/dist/index-FxeUTEK6.d.mts
+++ b/dist/index-FxeUTEK6.d.mts
@@ -16,6 +16,7 @@ interface CibaOptions {
     scope: string;
     bindingMessage?: string;
     authorizationDetails?: unknown;
+    acrValues?: string;
     approvalUrl: string;
   }, request?: Request) => Promise<void>;
   /**
@@ -114,6 +115,10 @@ declare const ciba: (options: CibaOptions) => {
           type: "string";
           required: false;
         };
+        acrValues: {
+          type: "string";
+          required: false;
+        };
         status: {
           type: "string";
           required: true;
@@ -177,6 +182,7 @@ declare const ciba: (options: CibaOptions) => {
       client_notification_uri?: string | undefined;
       authorization_details?: string | undefined;
       resource?: string | undefined;
+      acr_values?: string | undefined;
       requested_expiry?: unknown;
     }, Record<string, any> | undefined, [], Record<string, unknown>, {
       allowedMediaTypes: string[];
@@ -215,6 +221,7 @@ declare const ciba: (options: CibaOptions) => {
       scope: string;
       binding_message: string | undefined;
       authorization_details: unknown;
+      acr_values: string | undefined;
       status: string;
       expires_at: string;
     }, {
diff --git a/dist/index.mjs b/dist/index.mjs
index 1ce6454..447ba34 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -264,6 +264,7 @@ function createBcAuthorize(options) {
 			client_notification_uri: z.string().url().optional(),
 			authorization_details: z.string().optional(),
 			resource: z.string().optional(),
+			acr_values: z.string().optional(),
 			requested_expiry: z.coerce.number().int().positive().optional()
 		}),
 		metadata: {
@@ -363,6 +364,7 @@ function createBcAuthorize(options) {
 				bindingMessage: ctx.body.binding_message,
 				authorizationDetails: ctx.body.authorization_details,
 				resource: ctx.body.resource,
+				acrValues: ctx.body.acr_values,
 				status: "pending",
 				deliveryMode: mode,
 				clientNotificationToken: ctx.body.client_notification_token,
@@ -380,6 +382,7 @@ function createBcAuthorize(options) {
 			scope: ctx.body.scope,
 			bindingMessage: ctx.body.binding_message,
 			authorizationDetails: parsedAuthorizationDetails,
+			acrValues: ctx.body.acr_values,
 			approvalUrl
 		}, ctx.request).catch(() => {});
 		const response = {
@@ -610,6 +613,10 @@ const schema = { cibaRequest: {
 			type: "string",
 			required: false
 		},
+		acrValues: {
+			type: "string",
+			required: false
+		},
 		status: {
 			type: "string",
 			required: true
@@ -694,6 +701,7 @@ function createCibaVerify() {
 			scope: cibaRequest.scope,
 			binding_message: cibaRequest.bindingMessage,
 			authorization_details: parsedAuthorizationDetails,
+			acr_values: cibaRequest.acrValues,
 			status: cibaRequest.status,
 			expires_at: cibaRequest.expiresAt.toISOString()
 		});
